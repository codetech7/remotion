# Render Video Workflow

name: Render Video

on:
  push:
    branches:
      - main

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Render video
        run: npx remotion render src/index.tsx CodingEvolution out/video.mp4 --codec h264 --quality 80 --overwrite
